## 2026-04-21

- Bootstrap fallback order matters: prefer `NONFOOD_R2_OBJECT_KEY` only when R2 is actually configured, then `NONFOOD_EXCEL_PATH`, then the default local workbook path `ORDER NONFOOD MIN xlsx.xlsx`.
- Success must call the non-food loader and populate both `nonfood_items` and `nonfood_categories`; checking workbook availability alone is not enough.
- The non-food contract is safest when it is explicit in code: a dedicated enum block, a fixed callback prefix set, and dedicated session keys make later handlers easier to route without accidental food-flow collisions.
- Contract tests should import from the repo root explicitly when they are meant to be run directly from `tests/`; that keeps the namespace assertions stable under pytest.
- Non-food persistence works best when it mirrors the food document shape exactly (`date`/`items`/`updated_at` for orders and `_id`/`name`/`items`/`updated_at` for templates) while pointing at `nonfood_orders` and `nonfood_templates`.
- Isolation tests are most convincing when they assert the food collections have zero calls after exercising the non-food APIs.
- The safest `/order_nonfood` reset pattern is key-by-key cleanup of the known `nonfood_*` and `nf_*` session keys; clearing all `ctx.user_data` would wipe active food state because the bot conversation already allows re-entry.
- Entry-flow tests are more reliable when they load `nonfood_entry.py` directly from its file path instead of importing through `handlers.conversation`, because the package `__init__` eagerly imports the whole food conversation tree.
- Non-food `show_items` handler must explicitly detect BOTH `nfitem:back` (back from items → cats) AND `nfcat:back` (back from cats → editing). The `nfcat:` prefix replaces on `nfcat:back` give "back" — so the back check must be `raw == "nfcat:back"` or `raw == "nfitem:back"` BEFORE the prefix replace.
- Source badges `[CCDC]` / `[VTTH]` are rendered in button `text` labels, not in the message text. Tests must check `button.text` via `_button_texts(markup)` rather than the `edit_calls[-1]["text"]` message body.
- Test stubs for telegram.ext `ConversationHandler` must be real classes (not `type()` results) with `__init__` accepting `*a, **k`, otherwise instantiating `ConversationHandler(...)` in the module body fails. Same applies to `CallbackQueryHandler` and `MessageHandler`.
- The `show_cats` entry handler must `await q.answer()` when entered via callback (pattern `nf:add_item`), matching the `show_items` pattern. Without it, tests asserting `answer_calls` fail.
- The non-food `_show_nonfood_edit_screen` helper is defined locally in `nonfood_category.py` (not imported from `editing.py`) because `show_edit_screen` in editing returns `OrderStates.EDITING` (food), not `NONFOOD_EDITING`. This avoids touching the food editing module.
- Task 5 category browse: `nf:add_item` entry → shows cats → `nfcat:{cat}` → shows items with `[CCDC]`/`[VTTH]` badges → `nfitem:{code}` → prompts qty → `receive_qty` writes to `nonfood_order` with source, returns `NONFOOD_EDITING` via `_show_nonfood_edit_screen`.
- Task 5 created `_show_nonfood_edit_screen` which renders buttons with `nfe:edit:`, `nfe:confirm`, `nfe:save` callbacks — these are forward references to be wired in Tasks 7-9.
- `excel_service.py` was missing `load_items_nonfood` and `build_order_excel_nonfood` methods that Tasks 1-4 claimed were added. Added manually: `load_items_nonfood` reads CCDC+VTTH sheets with source field; `build_order_excel_nonfood` writes code→B, qty→G to PR NOODLE sheet rows 16+.
- Task 6 search: `NONFOOD_SEARCHING` state does NOT exist in states.py — added as state 25. Search flow: `nf:search` entry → `NONFOOD_SEARCHING` (code input) → `NONFOOD_ENTERING_QTY` (qty input) → `NONFOOD_EDITING` via `map_to_parent`.
- Search `receive_search_code` is structured as a MessageHandler in `NONFOOD_SEARCHING` state, not a CallbackQueryHandler. It performs exact `items.get(code)` match (no substring/prefix), then transitions to `NONFOOD_ENTERING_QTY` via `ask_search_qty`. Invalid code → reprompt staying in `NONFOOD_SEARCHING`.
- `ask_search_qty` is a local helper that mirrors the qty-prompt of `ask_qty` in nonfood_category but reads from the already-resolved `item` dict rather than from a callback query.
- `receive_search_qty` is adapted from `receive_qty` in nonfood_category — identical validation/flow but imported locally. `nf_current_item` is set by `ask_search_qty` before the state transition, so `receive_search_qty` works without modification.
- Back during code entry (`"back"` text) returns `ConversationHandler.END` directly, which maps to `NONFOOD_EDITING` via `map_to_parent`.
- Task 7 editing hub (`nonfood_editing.py`): central `nonfood_conv` top-level ConversationHandler wires `cmd_order_nonfood`, entry/history handlers, `nonfood_category_conv` and `nonfood_search_conv` as nested handlers, and `handle_nonfood_edit_menu`/`handle_nonfood_edit_qty`/`receive_nonfood_edit_qty` for editing.
- `handle_nonfood_edit_menu` pattern is `^nfei:` only (not `nfe:confirm`/`nfe:save`) to avoid colliding with `nfe:recent` (entry state callback) that would otherwise be caught by `nfe:` prefix in NONFOOD_EDITING state.
- `_show_nonfood_item_keypad` displays source badge in the keypad message text (not just button), using `f" [{item.get('source','')}]"` appended to the header.
- `_nonfood_done_editing` guards against empty `nonfood_order` — reply error + stay in `NONFOOD_EDITING`; non-empty → forward to `NONFOOD_CONFIRM_ORDER`.
- Task 7 added `CommandHandler` stub to `_install_telegram_stubs` (was missing before — only `CallbackQueryHandler`/`MessageHandler`/`ConversationHandler` were stubbed).
- `NONFOOD_SEARCHING` state 25 was added to states.py in Task 6, needed by nonfood_search.py.
- Task 8 template: `show_nonfood_template_menu` shows existing templates + "Tạo mẫu mới" + "Quay lại"; `handle_nonfood_template` routes `nftpl:new`/`nftpl:{id}`/`nftpl:back`/`nftpl:overwrite:{id}`.
- `receive_nonfood_template_name` checks if name matches existing via `list_nonfood_templates()` iteration (not `get_nonfood_template`) to avoid needing MongoDB in tests.
- `get_nonfood_template(template_id)` returns `list[dict]` (the items list), NOT the template doc — so `template['name']` access fails. Use `list_nonfood_templates()` to find the template doc by `_id` for the name.
- `save_nonfood_template(name_or_id, items)` saves by `_id` as the key — the first arg is the template `_id` (e.g. `"tpl-vp"`), not the display name. Tests assert against `template_id` not template name.
- Task 8 wiring: `nfe:save` in `NONFOOD_EDITING` now calls `show_nonfood_template_menu` (not placeholder); `NONFOOD_ENTERING_TEMPLATE_NAME` state handlers replaced stub with `CallbackQueryHandler(handle_nonfood_template, pattern=r"^nftpl:")` + `MessageHandler(..., receive_nonfood_template_name)`.
- Task 9 confirm: `show_nonfood_confirm_screen` renders items with `[CCDC]`/`[VTTH]` badges in message text; `receive_nonfood_confirm` routes `nfc:edit`→NONFOOD_EDITING, `nfc:date`→date menu, `nfc:cancel`→END, `nfc:confirm`→export. Date quick picks via `nfd:{iso}` callbacks; custom input via `DD/MM/YYYY` text in `NONFOOD_ENTERING_DATE`.
- Task 9 wiring: `NONFOOD_CONFIRM_ORDER` stub replaced with `CallbackQueryHandler(receive_nonfood_confirm, pattern=r"^nfc:")`; `NONFOOD_ENTERING_DATE` stub replaced with `CallbackQueryHandler(handle_nonfood_date_choice, pattern=r"^nfd:")` + `MessageHandler(filters.TEXT, receive_nonfood_date)`.
- `_do_nonfood_export`: reads `nonfood_excel_service` from `ctx.bot_data`, calls `build_order_excel_nonfood(items, date)`, saves via `save_nonfood_order(date, items)`, sends document via `InputFile(buf, filename=...)`, resets session via `NONFOOD_SESSION_KEYS`.
- Test stubs: `InlineKeyboardButton` must accept `*a, **k` because code passes positional args `InlineKeyboardButton(text, callback_data)`. `InputFile` stub added to telegram mock with `__init__` accepting buffer/filename kwargs.
- `io.BytesIO` used as buffer for Excel tests — openpyxl calls `flush()` during save, which `io.BytesIO` has but a custom class without `flush()` would fail.
- openpyxl returns `datetime.datetime` (not `date`) when reading back cell values — compare with `cell_date.date()` when asserting against `date` objects.
- Regression test: food handler references checked via AST (`isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef))`) to avoid triggering telegram module imports in test environment.
- Regression test: `bot.py` ConversationHandler count verified via `source.count("ConversationHandler(") == 1` to ensure no duplicate food handlers introduced.
